<%= form_for @breed, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@breed.errors.count, "error") %> prohibited this breed from being saved:</h2>

      <ul>
      <% @breed.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<fieldset>
    <legend><%= controller.action_name.capitalize %> Breed</legend>
	  <div class="control-group">
	    <%= f.label :name, :class => 'control-label' %><br />
			<div class="controls">
	    	<%= f.text_field :name, :class => 'string_select' %>
			</div>
	  </div>
	
	  <div class="form-actions">
      <%= f.submit nil, :class => 'btn btna-primary' %>
      <%= link_to 'Cancel', breeds_path, :class => 'btn' %>
    </div>
	</fieldset>
<% end %>